<section id="portfolio" class="portfolio-section section-padding alternate-bg">
  <div class="container">
    <h2 class="section-title">Portfolio Progetti</h2>
    <div class="portfolio-grid">
      {% for item in site.data.portfolio %}
        <div class="portfolio-item">
          <div class="portfolio-image">
            <img src="{{ item.image | relative_url }}" alt="Anteprima {{ item.title }}">
          </div>
          <div class="portfolio-info">
            <h3>{{ item.title }}</h3>
            <p>{{ item.description }}</p>
            {% if item.tags %}
            <div class="portfolio-tags">
              {% for tag in item.tags %}
                <span class="tag">{{ tag }}</span>
              {% endfor %}
            </div>
            {% endif %}
            {% if item.url and item.url != "#" %}
              <a href="{{ item.url }}" class="btn btn-sm btn-outline" target="_blank" rel="noopener noreferrer">Vedi Progetto</a>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>